import { FilterDeltaPipe } from './filter-delta.pipe';

describe('FilterDeltaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDeltaPipe();
    expect(pipe).toBeTruthy();
  });
});
