import { FilterPlusPipe } from './filter-plus.pipe';

describe('FilterPlusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlusPipe();
    expect(pipe).toBeTruthy();
  });
});
